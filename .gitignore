# Source assets — the app is fully self-contained in index.html.
# These are large originals that should NOT be published to the public repo.
Background App.png
*.png
*.jpg
*.jpeg
*.b64
*.pdf

# ...but DO publish the home-screen / PWA icons (needed for iOS "Add to Home Screen").
!apple-touch-icon.png
!icon-192.png
!icon-512.png
